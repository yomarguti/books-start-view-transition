---
import BookItem from "./BookItem.astro";
import { listOfBooks } from "../data/books";
---

<ul class="grid grid-cols-2 md:grid-cols-3 gap-3 px-4">
  {
    listOfBooks.map((book) => <BookItem {...book} />)
  }
</ul>
